# 支持properties/yml类型的配置文件, 默认会读取resources 或 resources/config下的配置文件
# 应用(服务)名
spring.application.name=eureka
# 启动端口
server.port=8761
# 获取注册信息, eureka本身为注册中心, 不需要获取注册中心
eureka.client.fetch-registry=false
# 注册到注册中心
eureka.client.register-with-eureka=false
